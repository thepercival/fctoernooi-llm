# Copy this file to .env for local development.
PORT=8080

# Base URL of the FCToernooi API (via APIM in Azure, or http://localhost:8080 for local backend)
FCTOERNOOI_API_BASEURL=http://localhost:8080

# Only needed for authenticated API calls; the tournament shells list uses the public endpoint
FCTOERNOOI_API_KEY=
